sequenceDiagram
    participant browser
    participant server

    Note over browser: User types a note and clicks Save

    Note over browser: JavaScript handles the submit event, prevents page reload, adds note to list, and rerenders DOM

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    Note over browser: Request Body: { "content": "SPA note creation", "date": "2026-09-13..." }
    activate server
    server-->>browser: HTTP 201 Created ({ "message": "note created" })
    deactivate server